# -*- coding: utf-8 -*-
class superdivision(type):
    """
    Stores, in variables with given names, a list of elements of a given class. This is a required parameter in the
    init of the class

    Provides:
        + get_subdivisions([type]) -- Returns all subdivisions
    """
    # Modifica init

    # funzione get_subdivisions('tipo')

    # funzione get_all_children('nome classe Target')
